#include "main.h"
/**
 * _strncpy - copy exactly n bytes to dest
 * @dest: destination text
 * @src: source text
 * @n: number of bytes
 *
 * Return: pointer to dest
 */
char *_strncpy(char *dest, char *src, int n)
{
	return (strnpcy(dest, src, n));
}
